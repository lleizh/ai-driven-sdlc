name: Update Feature Status on Merge

on:
  push:
    branches:
      - develop

jobs:
  update-status:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 # 前のコミットと比較するため

      - name: Extract Feature ID from merge commit
        id: extract
        run: |
          # 最新のコミットメッセージを取得
          COMMIT_MSG=$(git log -1 --pretty=%B)
          echo "Commit message: $COMMIT_MSG"

          # PR マージかチェック（通常のマージコミットまたは Squash merge）
          IS_PR_MERGE=false
          MERGE_TYPE=""

          # ケース1: 通常のマージコミット（2つの親を持つ）
          if git log -1 --pretty=%P | grep -q ' '; then
            echo "✓ マージコミットです（Create a merge commit）"
            IS_PR_MERGE=true
            MERGE_TYPE="merge"
          # ケース2: Squash merge（コミットメッセージに (#数字) を含む）
          elif echo "$COMMIT_MSG" | grep -qE '\(#[0-9]+\)'; then
            echo "✓ Squash merge です"
            IS_PR_MERGE=true
            MERGE_TYPE="squash"
          fi

          if [[ "$IS_PR_MERGE" == "false" ]]; then
            echo "⚠️ PR マージではありません。スキップします。"
            echo "is_merge=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # マージ元のブランチ名を取得
          BRANCH_NAME=""

          # パターン1: "Merge pull request #123 from owner/feature/FEATURE-19"
          if echo "$COMMIT_MSG" | grep -qE 'Merge pull request.*from.*feature/FEATURE-[0-9]+'; then
            BRANCH_NAME=$(echo "$COMMIT_MSG" | grep -oE 'feature/FEATURE-[0-9]+' | head -1)
          # パターン2: "Merge branch 'feature/FEATURE-19' into develop"
          elif echo "$COMMIT_MSG" | grep -qE "Merge branch 'feature/FEATURE-[0-9]+'"; then
            BRANCH_NAME=$(echo "$COMMIT_MSG" | grep -oE "feature/FEATURE-[0-9]+" | head -1)
          # パターン3: Squash merge - "FEATURE-19: タイトル (#123)"
          elif echo "$COMMIT_MSG" | grep -qE '^FEATURE-[0-9]+:.*\(#[0-9]+\)'; then
            BRANCH_NAME=$(echo "$COMMIT_MSG" | grep -oE '^FEATURE-[0-9]+' | sed 's/^/feature\//')
          fi

          if [[ -z "$BRANCH_NAME" ]]; then
            echo "⚠️ feature ブランチからのマージではありません。スキップします。"
            echo "is_merge=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "✓ マージ元ブランチ: $BRANCH_NAME"
          echo "✓ マージタイプ: $MERGE_TYPE"

          # Strict pattern で Feature ID を抽出: ^feature/FEATURE-[0-9]+$
          if [[ $BRANCH_NAME =~ ^feature/(FEATURE-[0-9]+)$ ]]; then
            FEATURE_ID="${BASH_REMATCH[1]}"
            echo "✓ Feature ID を抽出: $FEATURE_ID"
            echo "is_merge=true" >> $GITHUB_OUTPUT
            echo "feature_id=$FEATURE_ID" >> $GITHUB_OUTPUT
          else
            echo "❌ ブランチ名が命名規則に違反しています: $BRANCH_NAME"
            echo "❌ 期待される形式: feature/FEATURE-[0-9]+ (例: feature/FEATURE-19)"
            echo "is_merge=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Update metadata STATUS
        if: steps.extract.outputs.is_merge == 'true'
        run: |
          FEATURE_ID="${{ steps.extract.outputs.feature_id }}"
          METADATA_FILE="sdlc/features/$FEATURE_ID/.metadata"

          echo "=========================================="
          echo "Feature STATUS を更新中: $FEATURE_ID"
          echo "=========================================="

          # .metadata ファイルの存在確認
          if [[ ! -f "$METADATA_FILE" ]]; then
            echo "❌ エラー: .metadata ファイルが見つかりません"
            echo "❌ パス: $METADATA_FILE"
            echo ""
            echo "📋 手動リカバリー手順:"
            echo "1. $METADATA_FILE が存在するか確認"
            echo "2. 存在しない場合は /sdlc-init で Feature を初期化"
            echo "3. 存在する場合は手動で STATUS を completed に更新"
            exit 1
          fi

          echo "✓ .metadata ファイルを確認"

          # 現在の STATUS を確認
          CURRENT_STATUS=$(grep "^STATUS=" "$METADATA_FILE" | cut -d= -f2)
          echo "現在の STATUS: $CURRENT_STATUS"

          # STATUS が implementing でない場合は警告
          if [[ "$CURRENT_STATUS" != "implementing" ]]; then
            echo "⚠️ 警告: STATUS が implementing ではありません (現在: $CURRENT_STATUS)"
            echo "⚠️ completed への更新をスキップします"
            echo ""
            echo "📋 予想される原因:"
            echo "- Feature がまだ実装開始されていない (STATUS=planning または design)"
            echo "- すでに completed に更新済み"
            echo "- 手動で STATUS が変更された"
            exit 0
          fi

          # STATUS を implementing → completed に更新
          # awk を使用してポータブルに実装
          awk '{if ($0 ~ /^STATUS=implementing$/) print "STATUS=completed"; else print}' "$METADATA_FILE" > "$METADATA_FILE.tmp"
          mv "$METADATA_FILE.tmp" "$METADATA_FILE"

          # 更新を確認
          NEW_STATUS=$(grep "^STATUS=" "$METADATA_FILE" | cut -d= -f2)

          if [[ "$NEW_STATUS" != "completed" ]]; then
            echo "❌ エラー: STATUS の更新に失敗しました"
            echo "❌ 更新後の STATUS: $NEW_STATUS"
            exit 1
          fi

          echo "✓ STATUS を更新: $CURRENT_STATUS → $NEW_STATUS"

          # Git で変更をコミット
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "$METADATA_FILE"
          git commit -m "chore: Mark $FEATURE_ID as completed [skip ci]"

          # develop にプッシュ
          echo "変更を develop にプッシュ中..."
          git push origin develop

          echo ""
          echo "=========================================="
          echo "✅ $FEATURE_ID の STATUS を completed に更新しました"
          echo "=========================================="
          echo ""
          echo "次のステップ:"
          echo "- sync-projects.yml が自動的にトリガーされます"
          echo "- GitHub Projects の Status が 'Done' に更新されます"
